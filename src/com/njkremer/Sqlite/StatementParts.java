package com.njkremer.Sqlite;

/**
 * An enum of the different parts of a sql statement.
 */
public enum StatementParts {
    SELECT,
    UPDATE,
    INSERT,
    DELETE,
    JOIN,
    SET,
    FROM,
    WHERE,
    AND,
    ORDER_BY,
    COLUMN
}
